{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (F#)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin/Debug/net8.0/win-x64/McpFilesystemFs.dll",
            "args": ["${workspaceFolder}/test-dir"],
            "cwd": "${workspaceFolder}",
            "console": "internalConsole",
            "stopAtEntry": false
        }
    ]
}

